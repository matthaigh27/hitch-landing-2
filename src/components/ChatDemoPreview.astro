---
import ChatMockup from "./ChatMockup.astro";
---

<section class="chat-demo" id="chat-demo">
    <div class="chat-demo__inner">
        <h2 class="chat-demo__heading">
            The conversation ends.<br />
            <em>The context never does.</em>
        </h2>

        <figure class="chat-demo__figure">
            <ChatMockup />

            <figcaption class="chat-demo__caption">
                Hitch - branching threads, AI-extracted actions, zero
                notification noise
            </figcaption>
        </figure>
    </div>
</section>

<style>
    .chat-demo {
        padding: 6rem var(--gutter);
        border-top: 1px solid var(--divider);
    }

    .chat-demo__inner {
        max-width: var(--max-w);
        margin: 0 auto;
    }

    .chat-demo__heading {
        font-family: var(--font-display);
        font-size: var(--text-3xl);
        font-weight: 400;
        line-height: 1.15;
        letter-spacing: -0.02em;
        margin-bottom: 3rem;
        max-width: 22ch;
    }

    .chat-demo__heading em {
        font-style: italic;
        color: var(--accent);
    }

    .chat-demo__figure {
        border: 1px solid var(--divider);
        border-radius: 8px;
        overflow: hidden;
    }

    .chat-demo__caption {
        padding: 1rem 1.25rem;
        border-top: 1px solid var(--divider);
        font-family: var(--font-mono);
        font-size: var(--text-xs);
        color: var(--text-muted);
        background: var(--surface);
    }
</style>
