---
---

<section class="demo" id="demo">
  <div class="demo__inner">
    <h2 class="demo__heading">
      A guide, written and published<br />
      <em>while you were merging.</em>
    </h2>

    <figure class="demo__figure">
      <!-- ASSET NEEDED: Replace with PNG/WebP screenshot (min 1600px wide)
           OR <video autoplay muted loop playsinline> for GIF-like demo.
           Path: src/assets/guide-preview.png -->
      <div class="demo__placeholder">
        <span class="demo__placeholder-icon">◈</span>
        <p class="demo__placeholder-text">
          demo screenshot<br />
          <span class="demo__placeholder-sub">guide-preview.png · 1600px wide</span>
        </p>
      </div>

      <figcaption class="demo__caption">
        Hitch-generated guide — v2.4.1 · published in 4 min 12 sec
      </figcaption>
    </figure>
  </div>
</section>

<style>
  .demo {
    padding: 6rem var(--gutter);
    border-top: 1px solid var(--divider);
  }

  .demo__inner {
    max-width: var(--max-w);
    margin: 0 auto;
  }

  .demo__heading {
    font-family: var(--font-display);
    font-size: var(--text-3xl);
    font-weight: 400;
    line-height: 1.15;
    letter-spacing: -0.02em;
    margin-bottom: 3rem;
    max-width: 22ch;
  }

  .demo__heading em {
    font-style: italic;
    color: var(--accent);
  }

  .demo__figure {
    border: 1px dashed var(--divider);
    border-radius: 8px;
    overflow: hidden;
  }

  .demo__placeholder {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1.25rem;
    min-height: 480px;
    background: var(--surface);
    color: var(--text-muted);
    font-family: var(--font-mono);
  }

  .demo__placeholder-icon {
    font-size: 3rem;
    line-height: 1;
    color: var(--divider);
  }

  .demo__placeholder-text {
    text-align: center;
    font-size: var(--text-sm);
    line-height: 1.8;
  }

  .demo__placeholder-sub {
    color: var(--divider);
    font-size: var(--text-xs);
  }

  .demo__caption {
    padding: 1rem 1.25rem;
    border-top: 1px dashed var(--divider);
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    color: var(--text-muted);
    background: var(--surface);
  }
</style>
